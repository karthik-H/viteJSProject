# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: "ai_seed_test — confirm promptfoo reaches the LiteLLM gateway"

providers: file://provider.yaml

prompts:
  - "Say hi."

tests:
  - description: "each model returns a non-empty response"
    assert:
      - type: javascript
        value: output.trim().length > 0
